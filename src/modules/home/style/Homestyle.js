//importamos el style 
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    title: {
        fontSize: 24,
        fontWeight: 'bold',
        marginBottom: 20,
    },
    resumen: {
        marginTop: 20,
        fontSize: 18,
    },
    resumenItem: {
        fontSize: 16,
        marginVertical: 5,
    },
});